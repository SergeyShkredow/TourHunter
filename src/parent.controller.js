app.controller('ParentCtrl', function($scope, $timeout, mapService) {

    // $scope.$on('child', function (event, data) {
    //     console.log(data); // 'Some data'
    // });
    // $scope.$broadcast('parent', data);
});